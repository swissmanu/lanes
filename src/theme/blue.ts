import { DefaultTheme } from "styled-components";
import sharedTheme from "./shared";

const blue: DefaultTheme = {
  ...sharedTheme,
  board: {
    background: "#0079BF",
  },
};
export default blue;
